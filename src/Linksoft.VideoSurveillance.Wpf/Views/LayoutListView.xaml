<UserControl
    x:Class="Linksoft.VideoSurveillance.Wpf.Views.LayoutListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fontIcons="clr-namespace:Atc.Wpf.FontIcons;assembly=Atc.Wpf.FontIcons"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Linksoft.VideoSurveillance.Wpf.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModels:LayoutListViewModel}"
    Loaded="OnLoaded"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Title  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="24"
            FontWeight="SemiBold"
            Text="Layouts" />

        <!--  Toolbar  -->
        <ToolBar Grid.Row="1" Margin="0,0,0,8">
            <Button Command="{Binding AddLayoutCommand}" ToolTip="Add Layout">
                <StackPanel Orientation="Horizontal">
                    <fontIcons:ImageAwesomeSolid
                        Width="16"
                        Height="16"
                        Foreground="{DynamicResource AtcApps.Brushes.ThemeForeground}"
                        Icon="Plus" />
                    <TextBlock Margin="6,0,0,0" Text="Add Layout" />
                </StackPanel>
            </Button>
            <Separator />
            <Button Command="{Binding LoadCommand}" ToolTip="Refresh">
                <StackPanel Orientation="Horizontal">
                    <fontIcons:ImageAwesomeSolid
                        Width="16"
                        Height="16"
                        Foreground="{DynamicResource AtcApps.Brushes.ThemeForeground}"
                        Icon="SyncAlt" />
                    <TextBlock Margin="6,0,0,0" Text="Refresh" />
                </StackPanel>
            </Button>
        </ToolBar>

        <!--  Layout DataGrid  -->
        <DataGrid
            Grid.Row="2"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            IsReadOnly="True"
            ItemsSource="{Binding Layouts}"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Name}"
                    Header="Name" />
                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding GridDescription}"
                    Header="Grid Size" />
                <DataGridTextColumn
                    Width="120"
                    Binding="{Binding CameraCount}"
                    Header="Camera Count" />

                <!--  Action buttons  -->
                <DataGridTemplateColumn Width="140" Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button
                                    Margin="2"
                                    Padding="4,2"
                                    Command="{Binding DataContext.EditLayoutCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Edit">
                                    <fontIcons:ImageAwesomeSolid
                                        Width="14"
                                        Height="14"
                                        Foreground="{DynamicResource AtcApps.Brushes.ThemeForeground}"
                                        Icon="Edit" />
                                </Button>
                                <Button
                                    Margin="2"
                                    Padding="4,2"
                                    Command="{Binding DataContext.ApplyLayoutCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Apply">
                                    <fontIcons:ImageAwesomeSolid
                                        Width="14"
                                        Height="14"
                                        Foreground="{DynamicResource AtcApps.Brushes.ThemeForeground}"
                                        Icon="Check" />
                                </Button>
                                <Button
                                    Margin="2"
                                    Padding="4,2"
                                    Command="{Binding DataContext.DeleteLayoutCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"
                                    ToolTip="Delete">
                                    <fontIcons:ImageAwesomeSolid
                                        Width="14"
                                        Height="14"
                                        Foreground="OrangeRed"
                                        Icon="TrashAlt" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!--  Loading overlay  -->
        <Border
            Grid.Row="2"
            Background="#80000000"
            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="16"
                Foreground="White"
                Text="Loading layouts..." />
        </Border>
    </Grid>
</UserControl>