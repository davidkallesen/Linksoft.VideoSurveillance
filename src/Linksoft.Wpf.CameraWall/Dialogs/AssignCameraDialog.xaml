<atc:NiceWindow
    x:Class="Linksoft.Wpf.CameraWall.Dialogs.AssignCameraDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:atc="https://github.com/atc-net/atc-wpf/tree/main/schemas"
    xmlns:atcTranslation="https://github.com/atc-net/atc-wpf/tree/main/schemas/translations"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dialogs="clr-namespace:Linksoft.Wpf.CameraWall.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="{Binding Path=DialogTitle}"
    Width="700"
    Height="500"
    MinWidth="600"
    MinHeight="400"
    d:DataContext="{d:DesignInstance Type=dialogs:AssignCameraDialogViewModel,
                                     IsDesignTimeCreatable=False}"
    ResizeMode="CanResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    WindowStyle="ToolWindow"
    mc:Ignorable="d">

    <atc:GridEx Margin="16" Rows="Auto,*,Auto">

        <!--  Layout Name Header  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            HorizontalAlignment="Center"
            FontSize="22"
            FontWeight="SemiBold"
            Text="{Binding Path=LayoutName}" />

        <!--  Dual List Selector  -->
        <atc:DualListSelector
            Grid.Row="1"
            AllowDragDrop="True"
            AllowMultiSelect="True"
            AvailableHeader="{Binding Path=AvailableHeaderText}"
            AvailableItems="{Binding Path=AvailableItems, Mode=OneWay}"
            SelectedHeader="{Binding Path=SelectedHeaderText}"
            SelectedItems="{Binding Path=SelectedItems, Mode=OneWay}"
            ShowFilter="True"
            ShowItemCount="True"
            ShowReorderButtons="True"
            ShowTransferAllButtons="True" />

        <!--  Dialog Buttons  -->
        <atc:UniformSpacingPanel
            Grid.Row="2"
            Margin="0,16,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                MinWidth="80"
                Padding="16,8"
                Command="{Binding Path=OkCommand}"
                Content="{atcTranslation:Resx ResxName=Linksoft.Wpf.CameraWall.Resources.Translations,
                                              Key=Ok}"
                IsDefault="True" />
            <Button
                MinWidth="80"
                Padding="16,8"
                Command="{Binding Path=CancelCommand}"
                Content="{atcTranslation:Resx ResxName=Linksoft.Wpf.CameraWall.Resources.Translations,
                                              Key=Cancel}"
                IsCancel="True" />
        </atc:UniformSpacingPanel>
    </atc:GridEx>
</atc:NiceWindow>
