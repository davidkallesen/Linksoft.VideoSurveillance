<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:Linksoft.Wpf.CameraWall.Converters">

    <!--  Converters  -->
    <converters:BoolToOpacityConverter x:Key="BoolToOpacityConverter" />
    <converters:ConnectionStateToColorConverter x:Key="ConnectionStateToColorConverter" />
    <converters:ConnectionStateToTextConverter x:Key="ConnectionStateToTextConverter" />

    <!--  Fullscreen Close Button Style  -->
    <Style x:Key="FullScreenCloseButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#66000000" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="20">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#99FF0000" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>